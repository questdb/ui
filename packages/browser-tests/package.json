{
  "name": "browser-tests",
  "packageManager": "yarn@3.1.1",
  "private": true,
  "scripts": {
    "test": "cypress run --env failOnSnapshotDiff=false --env requireSnapshots=false --browser firefox",
    "test:update": "yarn run test --env updateSnapshots=true"
  },
  "devDependencies": {
    "@simonsmith/cypress-image-snapshot": "^9.0.3",
    "cypress": "^13.11.0"
  }
}
